#Kiarash

- add pkg: get google.uuid outside of service

- refactor user service depending on pgx (error handling)

- extract methods (in handler, service and repository)? too soon for that just now i think

- signup using two devices and integrate with them instead of shutting down one! (e.g. sharing session)

- duplicate code in user_cache_repo

#Saman